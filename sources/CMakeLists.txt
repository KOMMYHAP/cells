add_subdirectory(common)
add_subdirectory(processor)
add_subdirectory(simulation)
add_subdirectory(render)
add_executable(${PROJECT_NAME} main.cpp)
target_link_libraries(${PROJECT_NAME} PRIVATE cells-render)
